export const mdScreenQuery = { query: `(min-device-width: 768px)` };

export const lgScreenQuery = { query: `(min-device-width: 1024px)` };

export const xlScreenQuery = { query: `(min-device-width: 1280px)` };
